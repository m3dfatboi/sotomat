# 🏓 SotoPong

Рейтинговая система для настольного тенниса в офисе.
**FastAPI + PostgreSQL + React** — без лишних зависимостей.

### Возможности

- Рейтинг игроков (ELO-подобная система)
- История матчей с графиком рейтинга
- Турниры: одиночная олимпийская сетка с авто-сидингом по рейтингу
- Форматы турниров: денежные ставки или очки рейтинга
- BYE-проходы на всех этапах сетки
- Призовые места (победитель или топ-3)
- Таблица лидеров с фиксированной колонкой нумерации
- Сравнение двух игроков

---

## Быстрый старт (Docker Compose)

Рекомендуемый способ — поднять всё одной командой:

```bash
docker compose up -d --build
```

Открой в браузере: **http://localhost:8000**

База данных (PostgreSQL) хранится в Docker volume `postgres-data` и сохраняется между перезапусками.

```bash
# Логи
docker compose logs -f

# Остановить
docker compose down
```

---

## Запуск без Docker

### 1. Подними PostgreSQL

Например, через Docker:

```bash
docker run -d --name sotopong-db \
  -e POSTGRES_USER=sotopong \
  -e POSTGRES_PASSWORD=sotopong \
  -e POSTGRES_DB=sotopong \
  -p 5432:5432 postgres:16-alpine
```

### 2. Установи зависимости

```bash
pip install -r requirements.txt
```

### 3. Запусти сервер

```bash
DATABASE_URL=postgresql://sotopong:sotopong@localhost:5432/sotopong python server.py
```

Открой в браузере: **http://localhost:8000**

---

## Как расшарить коллегам (локальная сеть)

Если все в одной Wi-Fi сети (офис), коллегам не нужно ничего устанавливать.

1. Узнай свой локальный IP-адрес:
   - **Windows:** `ipconfig` → IPv4 Address
   - **Mac/Linux:** `ifconfig` или `ip addr` → inet

2. Запусти через Docker Compose (он уже слушает `0.0.0.0`):

   ```bash
   docker compose up -d --build
   ```

3. Скинь коллегам ссылку: `http://ТВО_IP:8000`

---

## Деплой в интернет

### Вариант A — Railway (бесплатно)

1. Зарегистрируйся на [railway.app](https://railway.app)
2. Создай новый проект → Deploy from GitHub
3. Добавь PostgreSQL плагин в Railway
4. Укажи `DATABASE_URL` из Railway в переменных окружения
5. Получишь ссылку вида `sotopong.up.railway.app`

### Вариант B — VPS с Docker

```bash
git clone <repo>
cd sotomat
docker compose up -d --build
```

### Вариант C — systemd (без Docker)

```ini
# /etc/systemd/system/sotopong.service
[Unit]
Description=SotoPong
After=network.target postgresql.service

[Service]
WorkingDirectory=/path/to/sotopong
Environment=DATABASE_URL=postgresql://sotopong:sotopong@localhost:5432/sotopong
ExecStart=/usr/bin/python3 server.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
systemctl enable sotopong
systemctl start sotopong
```

---

## Структура проекта

```
sotopong/
├── server.py           # FastAPI бэкенд (PostgreSQL)
├── requirements.txt    # Python зависимости
├── README.md
└── static/
    ├── index.html      # Точка входа, загружает скрипты
    ├── styles.css      # Все стили
    └── js/
        ├── utils.js        # Константы, apiFetch, общие стили
        ├── components.js   # Переиспользуемые компоненты (Avatar, PlayerSelect…)
        ├── modals.js       # Модальные окна (новый матч, игрок, турнир…)
        ├── pages.js        # Страницы (Лидерборд, История, Игроки, Сравнение)
        ├── tournament.js   # Турниры и сетка
        └── app.js          # Корневой App + рендер
```

---

## API

| Метод  | Путь                                    | Описание                        |
| ------ | --------------------------------------- | ------------------------------- |
| GET    | `/api/players`                          | Список игроков                  |
| POST   | `/api/players`                          | Добавить игрока                 |
| DELETE | `/api/players/{id}`                     | Удалить игрока                  |
| GET    | `/api/matches`                          | История матчей                  |
| POST   | `/api/matches`                          | Записать матч                   |
| DELETE | `/api/matches/{id}`                     | Удалить матч                    |
| GET    | `/api/tournaments`                      | Список турниров                 |
| POST   | `/api/tournaments`                      | Создать турнир                  |
| DELETE | `/api/tournaments/{id}`                 | Удалить турнир                  |
| POST   | `/api/tournaments/{id}/players`         | Добавить игрока в турнир        |
| DELETE | `/api/tournaments/{id}/players/{pid}`   | Удалить игрока из турнира       |
| POST   | `/api/tournaments/{id}/bracket`         | Сохранить состояние сетки       |
| POST   | `/api/tournaments/{id}/finish`          | Завершить турнир                |
